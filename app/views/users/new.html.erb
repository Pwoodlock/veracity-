<% content_for :head do %>
  <title>Add User - Server Manager</title>
<% end %>

<%= render layout: 'shared/sidebar' do %>

  <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <!-- Header -->
    <div class="mb-6">
      <h2 class="text-3xl font-bold text-base-content">Add New User</h2>
      <p class="text-base-content/80 mt-1">Create a new user account with role-based permissions</p>
    </div>

    <!-- Form Card -->
    <div class="bg-base-200 rounded-lg shadow-md border border-base-content/10 p-6">
      <%= render 'form', user: @user %>
    </div>
  </div>
<% end %>
