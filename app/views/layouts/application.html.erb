<!DOCTYPE html>
<html lang="en" data-theme="nord">
  <head>
    <title><%= content_for(:title) || "Veracity - Server Infrastructure Management" %></title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <!-- Favicon - Only show if custom logo is uploaded -->
    <% if SystemSetting.logo_url.present? %>
      <link rel="icon" href="<%= SystemSetting.logo_url %>" type="image/png">
      <link rel="apple-touch-icon" href="<%= SystemSetting.logo_url %>">
    <% end %>

    <!-- Inter Font (Modern Professional Typography) -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">

    <!-- Theme System: Initialize theme from localStorage before page loads to prevent flash -->
    <script>
      (function() {
        const theme = localStorage.getItem('daisyui-theme') || 'nord';
        document.documentElement.setAttribute('data-theme', theme);
      })();
    </script>

    <%# Local CSS - Tailwind + DaisyUI bundled %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%# Local JavaScript - All dependencies bundled %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

    <!-- Smooth theme transition CSS -->
    <style>
      .theme-transition {
        transition: background-color 0.3s ease, color 0.3s ease;
      }
      .theme-transition * {
        transition: background-color 0.3s ease, color 0.3s ease, border-color 0.3s ease;
      }
      .theme-option.active {
        background-color: hsl(var(--p) / 0.2);
        font-weight: 600;
      }
      .theme-option.active::before {
        content: '✓ ';
        margin-right: 0.5rem;
      }
    </style>
  </head>

  <body class="bg-base-300 text-base-content min-h-screen">
    <%= yield %>
  </body>
</html>
